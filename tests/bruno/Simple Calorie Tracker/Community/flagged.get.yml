info:
  name: Community Flagged List
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/api/community/flagged
  headers:
    - name: moderator-token
      value: "{{moderatorToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
